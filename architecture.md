# Архитектура проекта playwright-ai-auto-debug

## 🏗️ Архитектурные принципы

### 1. Модульность
- Каждый модуль отвечает за одну конкретную задачу
- Четкое разделение ответственности между компонентами
- Минимальная связанность между модулями

### 2. Конфигурируемость
- Все настройки выносятся в конфигурационный файл
- Значения по умолчанию для всех опциональных параметров
- Валидация обязательных параметров

### 3. Обработка ошибок
- Graceful degradation при отсутствии файлов
- Информативные сообщения об ошибках
- Продолжение работы при ошибках в отдельных файлах

## 📁 Структура проекта

```
playwright-ai-auto-debug/
├── bin/
│   └── index.js              # CLI точка входа
├── lib/
│   ├── index.js              # Основная логика
│   ├── config.js             # Управление конфигурацией
│   ├── extractPrompts.js     # Поиск файлов с ошибками
│   ├── sendToMistral.js      # Взаимодействие с AI API
│   └── updateHtml.js         # Обновление HTML отчетов
├── package.json
├── README.md
├── architecture.md
└── playwright.config.example.js
```

## 🔧 Технические решения

### Система конфигурации
- **Файл**: `lib/config.js`
- **Принцип**: Конфигурация через `playwright.config.js` пользователя
- **Fallback**: Значения по умолчанию для всех опциональных параметров
- **Валидация**: Проверка обязательных полей при загрузке

```javascript
// Структура конфигурации
ai_conf: {
  api_key: string,              // Обязательно
  ai_server: string,            // По умолчанию: 'https://api.mistral.ai'
  model: string,                // По умолчанию: 'mistral-medium'
  results_dir: string,          // По умолчанию: 'test-results'
  max_prompt_length: number,    // По умолчанию: 2000
  request_delay: number,        // По умолчанию: 1000
  messages: Array<Object>       // По умолчанию: системное сообщение
}
```

### Поиск файлов с ошибками
- **Файл**: `lib/extractPrompts.js`
- **Алгоритм**: Рекурсивный поиск `copy-prompt.txt` файлов
- **Путь**: Конфигурируемый через `results_dir`
- **Обработка**: Graceful handling отсутствующих директорий

### AI интеграция
- **Файл**: `lib/sendToMistral.js`
- **Протокол**: HTTP REST API с поддержкой streaming
- **Конфигурация**: Полностью конфигурируемый endpoint и модель
- **Rate limiting**: Настраиваемая задержка между запросами

### Обновление отчетов
- **Файл**: `lib/updateHtml.js`
- **Подход**: DOM manipulation через cheerio
- **Стиль**: Встроенные CSS стили для независимости
- **Безопасность**: Экранирование HTML контента

## 📋 Стандарты кодирования

### JavaScript/ES6+
- **Модули**: ES6 modules (`import/export`)
- **Async/Await**: Для всех асинхронных операций
- **Деструктуризация**: Использование где применимо
- **Template literals**: Для строк с переменными

### Именование
- **Функции**: camelCase, глаголы (`loadConfig`, `sendToMistral`)
- **Переменные**: camelCase, существительные (`apiKey`, `configPath`)
- **Константы**: UPPER_SNAKE_CASE (`DEFAULT_CONFIG`)
- **Файлы**: camelCase (`sendToMistral.js`)

### Документация
- **JSDoc**: Для всех публичных функций
- **Комментарии**: Объяснение сложной логики
- **README**: Актуальная документация использования
- **Примеры**: Рабочие примеры конфигурации

### Обработка ошибок
```javascript
// Стандартный паттерн обработки ошибок
try {
  const result = await riskyOperation();
  return result;
} catch (error) {
  if (error.code === 'EXPECTED_ERROR') {
    console.log('⚠️  Ожидаемая ошибка:', error.message);
    return defaultValue;
  }
  throw new Error(`Описательное сообщение: ${error.message}`);
}
```

### Логирование
- **Эмодзи**: Для визуального разделения типов сообщений
- **Уровни**: 
  - `🚀` - Запуск процесса
  - `⚙️` - Конфигурация
  - `🔍` - Поиск/анализ
  - `✅` - Успех
  - `⚠️` - Предупреждение
  - `❌` - Ошибка
  - `💾` - Сохранение данных

## 🔄 Жизненный цикл выполнения

1. **Инициализация** (`bin/index.js`)
   - Запуск CLI команды
   - Обработка глобальных ошибок

2. **Загрузка конфигурации** (`lib/config.js`)
   - Поиск `playwright.config.js`
   - Валидация и применение defaults
   - Построение сообщений для AI

3. **Поиск ошибок** (`lib/extractPrompts.js`)
   - Рекурсивный поиск в `results_dir`
   - Чтение содержимого `copy-prompt.txt`
   - Подготовка данных для обработки

4. **AI обработка** (`lib/sendToMistral.js`)
   - Отправка запроса в AI API
   - Обработка streaming ответа
   - Возврат полного решения

5. **Обновление отчетов** (`lib/updateHtml.js`)
   - Парсинг HTML отчетов
   - Добавление AI блока
   - Сохранение обновленного файла

## 🧪 Принципы тестирования

### Модульность тестов
- Каждый модуль тестируется независимо
- Мокирование внешних зависимостей
- Тестирование edge cases

### Интеграционные тесты
- Полный цикл выполнения
- Тестирование с реальными конфигурациями
- Проверка обработки ошибок

## 🔒 Безопасность

### API ключи
- Никогда не хардкодить в коде
- Хранение в конфигурации пользователя
- Маскирование в логах

### Валидация входных данных
- Проверка существования файлов
- Валидация конфигурации
- Санитизация HTML контента

## 📦 Управление зависимостями

### Принципы
- Минимальное количество зависимостей
- Использование только проверенных пакетов
- Регулярное обновление безопасности

### Текущие зависимости
- `cheerio` - DOM manipulation
- `dotenv` - Environment variables (legacy support)
- `glob` - File pattern matching (если используется)

## 🚀 Развертывание и публикация

### Версионирование
- Semantic Versioning (semver)
- MAJOR.MINOR.PATCH формат
- Changelog для каждой версии

### NPM публикация
- Проверка тестов перед публикацией
- Обновление документации
- Тегирование релизов в Git

### GitHub
- Структурированные commit messages
- Pull request для изменений
- Issues для отслеживания багов и фич 